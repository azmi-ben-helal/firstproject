{% extends "@App/portfolio/base2.html.twig" %}

{% block title %}AppBundle:User:update{% endblock %}

{% block body %}
<h1>Welcome to the User:update page</h1>

{% if user %}

    <table class="table">
        <thead>
        <tr>
            <th scope="col">Nom</th>
            <th scope="col">Prenom</th>
            <th scope="col">age</th>
            <th scope="col">Path</th>
        </tr>
        </thead>

            <tbody>
            <tr>
                <th scope="row">{{ user.nom }}</th>
                <td>{{ user.prenom }}</td>
                <td>{{ user.age }}</td>
                <td><img style=" border-radius: 50%;  width: 160px; height :160px;"


                         src="{{ asset(user.path|default('../images/newme.jpg')) }}"></td>

            </tbody>

    </table>
    {% else %}
    <di class="alert alert-danger">persone inexistante</di>
    {% endif %}

{% endblock %}
